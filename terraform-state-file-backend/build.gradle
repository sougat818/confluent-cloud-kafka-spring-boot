plugins {
      id "org.ysb33r.terraform"  version "0.9.0"
      id "org.ysb33r.terraform.wrapper"  version "0.9.0"

}

repositories {
    jcenter()
}

terraform {
    useAwsEnvironment() 
    variables {
        var 'prefix', 'confluent'
    }
}